# moon:
#   version: '2.0.0-beta.0'  # Cannot specify moon version here - use proto install in CI instead

proto:
  version: '0.54.1'

node:
  version: '22.20.0'
  packageManager: 'pnpm'

pnpm:
  version: '10.18.0'

go:
  version: '1.25.5'
